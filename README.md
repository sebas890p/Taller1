# Taller 1

## Integrantes

- Sara Bedoya
- Sebastian Pedroza

  
Firmas:

```
   // Firma de la función sin parámetros y sin valor de retorno
    void Saludar();

    // Firma de la función con parámetros y sin valor de retorno
    void Saludar(string nombre);

    // Firma de la función con parámetros y con valor de retorno
    int Sumar(int a, int b);

    // Firma de la función con tipo de retorno 'double' y sin parámetros
    double ObtenerPi();

    // Firma de la función con parámetros y con valor de retorno de tipo 'bool'
    bool EsMayorDeEdad(int edad);

    // Firma de la función con parámetros de diferentes tipos
    string CrearSaludo(string nombre, int edad);

    // Firma de la función con parámetros opcionales
    void Saludar(string nombre = "Invitado");

    // Firma de la función con múltiples parámetros y valor de retorno 'void'
    void ImprimirDetalles(string nombre, int edad, string ciudad);

    // Firma de la función recursiva
    int Factorial(int n);

    // Firma de la función con parámetros de tipo array
    int SumarNumeros(int[] numeros);
```

